#include <iostream>
using namespace std;

// STL = containers + algorithms + iterators
// containers -> object which stores data
    // 1.1 Sequence containers -> linear fashion (vector,list,dequeue)
    // 1.2 Associative containers -> direct access (set,multiset,map,multimap,dictionary)
    // 1.3 Derived containers -> real world modelling (stack,queue,priority queue)
// algorithms -> procedure to process data (sorting,searching..)
// iterators-> object which points to an element of a container

int main(){
    
    return 0;
}